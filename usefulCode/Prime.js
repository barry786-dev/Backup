/* function isPrime(num) {
    let i=1
  let start = 2;
  while (num > start) {
      console.log(i++)
    if (num % start++ === 0) {
      return false;
    }
  }
  return num > 1;
} */

/* function isPrime(num) {
    for (let i = 2; num > i; i++) {
      if (num % i == 0) {
        return false;
      }
    }
    return num > 1;
  } */
  /* function isPrime(num) {
    let start = 2;
    while (start <= Math.sqrt(num)) {
      if (num % start++ < 1) {
        return false;
      }
    }
    return num > 1;
  } */

  //console.log(isPrime(17));
